const styles = theme => ({
  header: {
    marginBottom: 20
  },
  subheader: {
    marginBottom: 20
  },
  input: {
    marginBottom: 20
  },
  paper: {
    marginTop: theme.spacing(3),
    marginBottom: theme.spacing(3),
    padding: theme.spacing(2)
  }
});
export default styles;
